[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Netlify serves at root — no base path needed
  # (GitHub Pages project sites need BASE_PATH, Netlify does not)
  BASE_PATH = ""
